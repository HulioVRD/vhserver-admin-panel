# Config

* `git clone git@github.com:HulioVRD/vhserver-admin-panel.git`
* `sudo apt install nodejs`
* `npm i`
* `npm run config`
* `npm i pm2@latest -g`
* `pm2 start npm -- start`
* `pm2 startup`
* `pm2 save`
